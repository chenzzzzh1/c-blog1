# C的博客

一个功能完整的全栈博客系统,包含文章发布、评论系统、用户认证、分类标签、搜索功能和管理后台。

## 功能特性

### 前端功能
- 📝 文章列表和详情页展示
- 🔍 文章搜索功能
- 🏷️ 分类和标签浏览
- 💬 评论系统(发表、回复、点赞)
- 👤 用户注册和登录
- 📊 个人中心(查看和编辑个人信息)
- 🔐 文章管理后台(仅管理员可用)

### 后端功能
- RESTful API 接口
- JWT 用户认证
- 用户权限管理
- 文章 CRUD 操作
- 分类和标签管理
- 评论系统
- 搜索功能

## 技术栈

### 前端
- Vue.js 3
- Vue Router
- Pinia (状态管理)
- Element Plus (UI组件库)
- Axios (HTTP客户端)
- Vite (构建工具)

### 后端
- Node.js
- Express.js
- MongoDB
- Mongoose (ODM)
- JWT (认证)
- bcryptjs (密码加密)

## 项目结构

```
my-blog/
├── backend/                 # 后端
│   ├── models/             # 数据模型
│   │   ├── User.js
│   │   ├── Article.js
│   │   ├── Comment.js
│   │   ├── Category.js
│   │   └── Tag.js
│   ├── routes/             # 路由
│   │   ├── auth.js
│   │   ├── users.js
│   │   ├── articles.js
│   │   ├── comments.js
│   │   ├── categories.js
│   │   ├── tags.js
│   │   └── search.js
│   ├── middleware/         # 中间件
│   │   └── auth.js
│   ├── server.js           # 服务器入口
│   ├── package.json
│   └── .env.example        # 环境变量示例
├── frontend/               # 前端
│   ├── src/
│   │   ├── api/           # API接口
│   │   ├── components/    # 组件
│   │   ├── router/        # 路由
│   │   ├── store/         # 状态管理
│   │   ├── utils/         # 工具函数
│   │   ├── views/         # 页面
│   │   ├── App.vue
│   │   └── main.js
│   ├── package.json
│   └── vite.config.js
└── README.md
```

## 安装和运行

### 前置要求
- Node.js (v14 或更高版本)
- MongoDB (v4.4 或更高版本)

### 后端安装

1. 进入后端目录:
```bash
cd backend
```

2. 安装依赖:
```bash
npm install
```

3. 配置环境变量:
复制 `.env.example` 为 `.env` 并修改配置:
```bash
cp .env.example .env
```

编辑 `.env` 文件:
```
PORT=5000
MONGODB_URI=mongodb://localhost:27017/myblog
JWT_SECRET=your-secret-key-change-this-in-production
NODE_ENV=development
```

4. 启动后端服务器:
```bash
npm run dev
```
或
```bash
npm start
```

### 前端安装

1. 进入前端目录:
```bash
cd frontend
```

2. 安装依赖:
```bash
npm install
```

3. 启动前端开发服务器:
```bash
npm run dev
```

## 使用说明

### 访问地址
- 前端: http://localhost:3000
- 后端API: http://localhost:5000

### 默认管理员账户
首次注册的用户默认为普通用户,如需管理员权限,请手动在数据库中将用户角色改为 'admin'。

### 主要功能

1. **注册和登录**
   - 访问 /register 进行注册
   - 访问 /login 进行登录

2. **发布文章**
   - 登录后访问 /admin 进入管理后台
   - 点击"新建文章"创建文章
   - 可以选择分类、标签,设置发布状态

3. **浏览文章**
   - 首页展示最新文章
   - 可以按分类或标签筛选文章
   - 使用搜索功能查找文章

4. **评论互动**
   - 登录后可以对文章发表评论
   - 点赞文章和评论

5. **个人中心**
   - 查看个人信息
   - 编辑个人简介
   - 查看自己发布的文章

## API 接口

### 认证相关
- POST /api/auth/register - 用户注册
- POST /api/auth/login - 用户登录
- GET /api/auth/me - 获取当前用户信息

### 文章相关
- GET /api/articles - 获取文章列表
- GET /api/articles/:id - 获取单篇文章
- POST /api/articles - 创建文章
- PUT /api/articles/:id - 更新文章
- DELETE /api/articles/:id - 删除文章
- POST /api/articles/:id/like - 点赞文章

### 评论相关
- GET /api/comments/article/:articleId - 获取文章评论
- POST /api/comments - 发表评论
- DELETE /api/comments/:id - 删除评论
- POST /api/comments/:id/like - 点赞评论

### 分类相关
- GET /api/categories - 获取所有分类
- POST /api/categories - 创建分类
- PUT /api/categories/:id - 更新分类
- DELETE /api/categories/:id - 删除分类

### 标签相关
- GET /api/tags - 获取所有标签
- POST /api/tags - 创建标签
- PUT /api/tags/:id - 更新标签
- DELETE /api/tags/:id - 删除标签

### 搜索相关
- GET /api/search?q=关键词 - 搜索文章

## 开发注意事项

1. **安全性**
   - 生产环境务必修改 JWT_SECRET
   - 使用 HTTPS
   - 实现请求限流

2. **数据库**
   - 确保 MongoDB 服务正在运行
   - 定期备份数据库

3. **部署**
   - 前端构建: `npm run build`
   - 使用 PM2 或类似工具管理后端进程
   - 配置 Nginx 反向代理

## 许可证

MIT License
